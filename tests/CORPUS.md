# Корпус проб arez — явный реестр (REQ-27)

Число названо ПОСЛЕ замера, не обещано: **59 проб**, 0 fail, 0 skip.
Замер: `node --test --test-reporter=tap 'src/**/*.test.ts'` → `# tests 59 / # pass 59 / # fail 0 / # skipped 0`.

Пробы лежат рядом с кодом (`src/*.test.ts`, идиома node --test: тест видит модуль через относительный
импорт, гейт гонит один glob). Этот файл — их явный реестр: что каждая группа доказывает.

| Файл | Проб | Что доказывает |
|---|---:|---|
| `src/observed-red.test.ts` | 10 | анти-театр (фаза B): RED→GREEN засчитан, полый отвергнут, фикс-не-закрыл не доказательство; невалидный код и до-фиксный тест, убитый сигналом → infra-failed, не proven |
| `src/doctor.test.ts` | 7 | дрейф ядра (REQ-41): зелёный на совпавших байтах, RED на подмене/пропаже/пустом пине/дубле; обязательный набор — ровно 5 примитивов |
| `src/catalog.test.ts` | 8 | инварианты сиденья: claude/top, daimons:[] (LIM-01), 6 сканеров (REQ-19), deimoz-оркестратор; null-JSON и плейбук вне санкц. каталога → RED |
| `src/phase-a.test.ts` | 7 | оркестрация recon→exploit→prove (REQ-28): 6 сканеров по порядку, proven→находка; теория/театр/открытая дыра и ПОДДЕЛАННЫЙ proof (вердикт из кодов) валят кампанию |
| `src/phase-c.test.ts` | 12 | спин-ап цели (REQ-30): assertLoopback режет LAN/пустой бинд, containment реален только под наблюдением, targetPolicy loopback без host-egress, fail-closed spinUp, отказ squatter'у по pid, идемпотентный teardown, своя seatbelt на darwin |
| `src/phase-d.test.ts` | 11 | scope-манифест (REQ-31): валиден только от координатора с корректными путями/портом, hit по объявленной цели, write под owns, traversal режется, недействительный манифест не даёт разрешений, неизвестный вид → deny, автономная цепочка, запрет спавна (MAX_DEPTH=1), D8-грант |
| `src/seat-adapter.test.ts` | 3 | arez→сиденье Ареса (REQ-03): дескриптор держит инварианты, сиденье ares≠deimoz (LIM-03), полу-собранный каталог не встаёт |
| `src/acceptance.test.ts` | 1 | сквозной сценарий Деймоза A-D (REQ-08): все четыре фазы зелены в обе стороны на реальном каталоге |
| **Итого** | **59** | |

Пробы намеренно чистые: исполнение (раннер тестов, spawn цели, канарейка, цепочка) инъектируется, поэтому
корпус гоняется без живой сети и живой цели, кросс-платформенно, 0 skip. Живой seatbelt проверяется как
утверждение в обе стороны (darwin → sandbox-exec, иначе → fail-closed SandboxUnavailable), не как пропуск.

13 из 59 проб — негативные, добавлены после кросс-модельного враждебного ревью кодом (codex, генератор≠верификатор):
каждый закрытый fail-open путь (невалидный код, убитый сигналом тест, подделанный proof, порт-squatter,
недействительный scope, пустой/дублированный пин, null-каталог, плейбук вне каталога) несёт свою пробу-контрпример.
